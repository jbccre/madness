explore_submitted_brackets <- function(input, output, session) {
  tagList(
    h3("Explore Submitted Brackets"),
    p("Placeholder: this will eventually show uploaded brackets, comparisons, or bracket stats.")
  )
}
